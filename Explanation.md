ex1-functions.js — ฟังก์ชันคำนวณคะแนน

เขียน 4 ฟังก์ชันหลักๆ

(1.1)  isValidScore เช็คว่าคะแนนที่ส่งเข้ามาโอเคไหม ต้องเป็นตัวเลขจริงๆ (ไม่ใช่ string หรือ NaN) แล้วต้องอยู่ในช่วง 0-100 ถ้าไม่ผ่านเงื่อนไขไหนเลยก็คืน false
(1.2)  toGrade แทนที่จะเขียน if-else ซ้อนกันเยอะๆ เลยทำเป็น array ของเกณฑ์เกรดไว้ก่อน เรียงจากคะแนนสูงสุด (A) ไปต่ำสุด (F) แล้วใช้ find ไล่หาตัวแรกที่คะแนนเราสูงกว่าหรือเท่ากับเกณฑ์นั้น พอเจอก็หยุดเลย ข้อดีคือถ้าจะเพิ่ม/แก้เกณฑ์เกรดทีหลัง แค่แก้ใน array ตัวเดียว ไม่ต้องไปไล่แก้ if หลายจุด
(1.3)  calculateWorkshopScore ใส่สูตรตามที่โจทย์ให้ตรงๆ คือเอาคะแนนดิบหารด้วยคะแนนเต็มแล้วคูณน้ำหนัก แต่ full กับ weight ตั้ง default ไว้เป็น 60 กับ 20 เผื่อคนเรียกฟังก์ชันไม่ได้ส่งค่ามา จะได้ไม่ต้องพิมพ์ซ้ำทุกครั้ง
(1.4)  calculateTotal เอาคะแนน 5 ส่วนมาบวกกันตรงๆ ไม่มีอะไรซับซ้อน

พอทดสอบกับข้อมูล 3 คน (ส่วนที่ 2) ก็โชว์ผลเป็นตาราง แล้วในส่วนที่ 3 ลองพิสูจน์ว่า default parameter ทำงานจริง โดยเทียบว่าเรียกแบบไม่ใส่ full/weight กับใส่ค่าเต็มๆ (60, 20) ได้ผลเท่ากันไหม แล้วก็ลองส่ง undefined เข้าไปตรงตำแหน่ง full ดูว่า JS จะไปเอา default (60) มาใช้แทนแม้เราจะ "ส่งอะไรมาสักอย่าง" ก็ตาม เพราะ default parameter จะทำงานเฉพาะตอนที่ค่าตรงนั้นเป็น undefined เท่านั้น

ex2-collection.js — ทะเบียนนักศึกษา

สร้างข้อมูลนักศึกษา 6 คนเป็น array ของ object ตั้งใจทำให้คนสุดท้ายไม่มี contact เลย เพื่อเอาไปทดสอบเคส edge case ทีหลัง

ฟังก์ชันค้นหาทั้งหมด:

(2.1)  findById ใช้ .find() เพราะเราต้องการแค่คนเดียว เจอก็คืนตัวนั้น ไม่เจอก็คืน undefined ให้เอง อันนี้เป็นพฤติกรรมปกติของ find อยู่แล้ว ไม่ต้องเขียนโค้ดกันเพิ่ม
(2.2)  findByMajor ใช้ .filter() เพราะต้องการหลายคนที่ตรงเงื่อนไข ไม่เจอเลยก็ได้ array ว่างกลับมา ไม่ error
(2.3)  hasFailingStudent ใช้ .some() เพราะแค่อยากรู้ว่า "มีไหม" ไม่ต้องรู้ว่าใครบ้าง คืนแค่ true/false
(2.4)  getEmail อันนี้ซับซ้อนสุด เพราะต้องกัน 2 เคส ทั้งหาคนไม่เจอเลย และเจอคนแต่ไม่มี contact เลยใช้ ?. (optional chaining) คอยกันไม่ให้พัง ถ้าตรงไหนเป็น undefined มันจะหยุดแล้วส่งต่อ undefined ไปเรื่อยๆ แทนที่จะ error แล้วปิดท้ายด้วย ?? (nullish coalescing) ถ้าสุดท้ายได้ undefined มาก็เปลี่ยนเป็นข้อความ "ไม่พบข้อมูลติดต่อ" แทน

ทดสอบด้วย id ปลอม '9999' กับคนที่ไม่มี contact ดูว่าโปรแกรมไม่ล่มเลย ตรงตามเงื่อนไขที่โจทย์ให้

ex3-transform.js — สรุปผลการเรียน

ข้อนี้บังคับห้ามใช้ for/while เลย ต้องใช้ map/filter/reduce ล้วนๆ

(3.1)  getNames ใช้ map แปลง array ของ object ให้เหลือแค่ชื่อ
(3.2)  getPassedStudents ใช้ filter กรองเอาเฉพาะคนคะแนน >= 50
(3.3)  getTotalScore ใช้ reduce สะสมผลรวมคะแนน ใส่ 0 เป็นค่าเริ่มต้นไว้กันกรณี array ว่าง
(3.4)  getAverageScore เช็คก่อนว่า array ว่างไหม ถ้าว่างคืน 0 ไปเลยกันหารด้วยศูนย์ ถ้าไม่ว่างก็เอาผลรวมมาหารจำนวนคน แล้วปัดทศนิยม 2 ตำแหน่งด้วย toFixed (แต่ toFixed คืนค่าเป็น string เลยต้องครอบด้วย Number อีกที)
(3.5)  countByGrade ใช้ reduce เหมือนกันแต่ตัวสะสมเป็น object แทนตัวเลข ไล่นับทีละคนว่าเกรดไหนมีกี่คน ถ้ายังไม่เคยเจอเกรดนั้นมาก่อนก็เริ่มนับที่ 0 ก่อนค่อย +1
(3.6)  getTopStudent ใช้ reduce แบบไม่ใส่ค่าเริ่มต้น เพราะอยากให้เอา "คนแรกในลิสต์" เป็นตัวตั้งต้นไปเลย แล้วไล่เทียบทีละคนว่าใครคะแนนสูงกว่า ถ้ามี array ว่างก็เช็คแยกไว้ก่อนแล้วคืน undefined กันไม่ให้ reduce error

ส่วนที่ 2 คือโจทย์ให้ต่อ filter → map → reduce เป็นสายเดียว หาค่าเฉลี่ยเฉพาะสาขา CE โดยกรองก่อน แล้วดึงเฉพาะคะแนน แล้วสะสมเป็นค่าเฉลี่ยในตาเดียว (หารทีละตัวก่อนบวก แทนที่จะบวกก่อนหารทีหลัง เพราะอยากให้จบใน reduce เดียวไม่มีตัวแปรกลาง)

ส่วนที่ 3 ทดสอบทุกฟังก์ชันด้วย array ว่าง [] ดูว่าไม่มีตัวไหน error เลย

ex4-destructuring.js — รวมข้อมูลจากหลายแหล่ง

โจทย์จำลองสถานการณ์ว่าข้อมูลนักศึกษาคนเดียวถูกเก็บกระจายอยู่ 3 ที่ (ข้อมูลพื้นฐาน, คะแนน, ข้อมูลติดต่อ)

(4.1)  mergeStudent ใช้ spread (...) รวม 3 object เข้าด้วยกันเป็นก้อนเดียว เรียงลำดับสำคัญมาก เพราะถ้ามี key ซ้ำกัน ตัวที่เขียนทีหลังจะทับตัวก่อนหน้าเสมอ แล้วเพิ่ม property grade เข้าไปด้วย
(4.2)  ส่วนพิสูจน์ shallow copy เป็นจุดที่งงสุด: ลอง spread ก้อนที่ merge แล้วออกมาเป็นตัวใหม่ (copy) แล้วลองแก้ค่า email ข้างในผ่าน copy ปรากฏว่าต้นฉบับ (merged) เปลี่ยนตามไปด้วย! เพราะ spread แบบตื้นๆ ({ ...merged }) มันคัดลอกแค่ชั้นบนสุด ส่วน object ที่ซ้อนอยู่ข้างใน (อย่าง contact) มันแค่คัดลอก "ที่อยู่อ้างอิง" มา ไม่ได้สร้างของใหม่จริงๆ พอแก้ผ่านตัวไหนเลยกระทบกันหมดเพราะจริงๆ มันชี้ไปที่ก้อนเดียวกัน วิธีแก้คือต้อง spread ชั้น contact แยกอีกทีด้วย ถึงจะแยกกันจริง
(4.3)  formatStudent ใช้ destructuring ดึง property ที่ต้องใช้ออกมาจาก object ตรงพารามิเตอร์เลย ไม่ต้องมานั่งเขียน student.name, student.score ทีละอัน แถมตั้ง default ให้ major ไว้เผื่อไม่มีค่าด้วย
(4.4)  ส่วน rest ใช้แยก contact ออกจากข้อมูลที่เหลือ เอาไปอธิบายเรื่อง privacy ว่าทำไม API จริงไม่ควรส่ง contact (อีเมล/เบอร์โทร) ออกไปให้ทุก endpoint เพราะเป็นข้อมูลส่วนตัว ควรส่งเฉพาะ field ที่หน้านั้นๆ ต้องใช้จริง

ex5-module — แยกเป็นโมดูล

เอาโค้ดจากข้อ 1-4 มาจัดระเบียบใหม่ แยกความรับผิดชอบเป็น 3 ไฟล์ชัดเจน

(5.1)  student-data.mjs ทำหน้าที่เป็น "แหล่งข้อมูล" อย่างเดียว เก็บ array นักศึกษาไว้ข้างในแบบไม่ export ตรงๆ (กันคนนอกมาแก้ของจริง) แล้ว export แค่ฟังก์ชันที่คืนสำเนาข้อมูลออกไปแทน
(5.2)  student-service.mjs เป็น "กฎทางธุรกิจ" ล้วนๆ อย่างการคิดเกรด เช็คว่าผ่านไหม สรุปผล ไฟล์นี้ตั้งใจไม่ให้ import อะไรเลย รับข้อมูลผ่าน parameter เท่านั้น เพื่อให้ทดสอบง่ายและเอาไปใช้กับข้อมูลจากไหนก็ได้ ไม่ผูกติดกับไฟล์ข้อมูลไฟล์เดียว
(5.3)  main.mjs เป็นไฟล์เดียวที่ import ทั้งสองไฟล์ข้างบนมาใช้งานจริง แล้วค่อย console.log แสดงผล เป็นไฟล์เดียวที่ "รัน" ได้

คำตอบ 3 ข้อท้ายไฟล์:

ถ้าจะเพิ่มเกรดใหม่ (เช่น B+) แก้แค่ student-service.mjs พอ เพราะเกณฑ์เกรดอยู่ในฟังก์ชัน toGrade ไฟล์เดียว ไม่เกี่ยวกับไฟล์อื่นเลย
ถ้าจะย้ายข้อมูลไปฐานข้อมูลจริง แก้แค่ student-data.mjs พอ เพราะเป็นไฟล์เดียวที่รู้ว่าข้อมูลมาจากไหน ไฟล์อื่นแค่เรียกใช้ฟังก์ชันชื่อเดิม ไม่ต้องรู้ว่าข้างในเปลี่ยนไปยังไง
ที่ student-service.mjs ห้าม import ไฟล์ข้อมูลเลย เพราะอยากแยกส่วน "กฎ" ออกจาก "ข้อมูลมาจากไหน" ให้เด็ดขาด ทำให้เทสง่ายกว่า และเอาไปใช้ซ้ำกับข้อมูลชุดอื่นได้โดยไม่ต้องแก้อะไรเลย